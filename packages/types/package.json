{
  "name": "@dl-chat/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for DL Chat",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
